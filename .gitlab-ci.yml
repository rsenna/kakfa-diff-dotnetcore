image : microsoft/dotnet:latest
stages:
  - build
before_script:
  - 'dotnet restore'
build:
 stage: build
 script:
  - 'dotnet build'
  - 'dotnet vstest ((ls -Recurse *.Test.Unit.dll | % FullName) -Match "\\bin\\Debug\\")'
 only:
   - master
