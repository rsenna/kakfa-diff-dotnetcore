image : microsoft/dotnet:latest
stages:
  - build
before_script:
  - 'dotnet restore'
build:
 stage: build
 script:
  - 'dotnet build'
  - 'find -wholename **/bin/Debug/*Test.Unit.dll | xargs dotnet vstest'
 only:
   - master
